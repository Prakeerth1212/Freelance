services:
  - type: web
    name: freelance-backend
    runtime: java
    region: oregon
    buildCommand: cd backend && mvn clean package -DskipTests
    startCommand: java -jar backend/target/freelancehub-backend-1.0.0.jar
    envVars:
      - key: DB_HOST
        fromDatabase:
          name: freelance-db
          property: host
      - key: DB_PORT
        fromDatabase:
          name: freelance-db
          property: port
      - key: DB_NAME
        fromDatabase:
          name: freelance-db
          property: database
      - key: DB_USER
        fromDatabase:
          name: freelance-db
          property: user
      - key: DB_PASSWORD
        fromDatabase:
          name: freelance-db
          property: password
      - key: GEMINI_API_KEY
        sync: false

databases:
  - name: freelance-db
    region: oregon
    databaseName: freelance
    user: freelance
    plan: free
